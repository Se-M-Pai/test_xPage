import 'package:flutter/material.dart';

class CartButtonOnCard extends StatelessWidget {
  const CartButtonOnCard({super.key});

  @override
  Widget build(final BuildContext context) {
    return const Placeholder();
  }
}
